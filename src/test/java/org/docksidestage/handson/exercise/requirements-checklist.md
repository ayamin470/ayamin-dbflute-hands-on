# 要件チェック

## exercise8-要件
対象: test_1974年までに生まれたもしくは不明の会員を検索()

- [ ] 1974年までに生まれた、もしくは不明の会員を検索できている
  - [x] Arrange で String の "1974/01/01" を一度宣言している
  - [x] Arrange で宣言した文字列を日付クラスに変換している
  - [x] 変換した日付クラスの値を、日付移動などせずそのまま使って検索条件を実現している
  - [x] 会員ステータス名称を取得してログ出力している
  - [x] リマインダ質問を取得してログ出力している
  - [x] リマインダ回答を取得してログ出力している
  - [x] 退会理由入力テキストを取得してログ出力している
  - [x] ログ出力で値がない項目は "none" を出力している
  - [x] ログ出力で "none" を出す実装に if 文を使っていない
  - [x] ログ出力で map() を使っている
  - [x] 生年月日が若い順で並べている
  - [x] 生年月日が null のデータを最初に並べている
  - [x] データが存在することをアサートしている
  - [x] 生年月日が指定された条件に合致することをアサートしている
    - [x] 1975年1月1日 生まれなら落ちる
    - [x] 1974年12月31日 生まれは落ちない
  - [x] Arrange で「きわどいデータ」を作っている
    - [x] きわどいデータの作成に Behavior の updateNonstrict() を使っている
    - [x] 1974年12月31日 生まれの会員を作っている
    - [x] 1975年1月1日 生まれの会員を作っている
    - [x] 1974年12月31日 生まれの会員が検索結果に含まれることをアサートしている
    - [x] 1975年1月1日 生まれの会員が検索結果に含まれないことをアサートしている

## exercise9-要件
対象: test_2005年6月に正式会員になった会員を先に並べて生年月日のない会員を検索()

- [x] 2005年6月に正式会員になった会員を先に並べて生年月日のない会員を検索できている
  - [x] Arrange で String の "2005/06/01" を一度宣言している
  - [x] Arrange で宣言した文字列を日付クラスに変換している
  - [x] 変換した日付クラスの値を、日付移動などせずそのまま使って検索条件を実現している
  - [x] 生年月日が存在しない会員を検索している
  - [x] 2005年6月に正式会員になった会員を先に並べている
  - [x] 第二ソートキーが会員IDの降順になっている
  - [x] データが存在することをアサートしている
  - [x] 検索された会員の生年月日が存在しないことをアサートしている
  - [x] 2005年6月に正式会員になった会員が先に並んでいることをアサートしている
    - [x] 先頭だけでなく全体をチェックしている

## exercise10-要件
対象: 全ての会員をページング検索()
前提：
- カウント検索：全部で何件あるかを知る
- 実データ検索：ページに表示する会員データそのものを取る

- [x] 全ての会員をページング検索できている
  - [x] 会員ステータス名称も取得している
  - [x] 会員IDの昇順で並べている
  - [x] ページサイズは 3、ページ番号は 1 で検索している(1ページ3件で、1ページ目を検索)
  - [x] 会員ID、会員名称、会員ステータス名称をログに出力している
  - [x] SQLログでカウント検索時と実データ検索時の違いを確認している
    - [x] カウント検索では件数取得用のSQLになっていることを確認している
      - select found_rows()になっている
    - [x] 実データ検索ではページサイズ分を取得するSQLになっていることを確認している
      -  limit 0, 3になっている
  - [x] 総レコード件数が会員テーブルの全件であることをアサートしている
  - [x] 総ページ数が期待通りのページ数であることをアサートしている
    - [x] 期待値を固定値ベタ書きではなく計算で導出している
  - [x] 検索結果のページサイズ、ページ番号が指定されたものであることをアサートしている
  - [x] 検索結果が指定されたページサイズ分のデータだけであることをアサートしている
  - [x] PageRange を 3 にして PageNumberList を取得していて、 PageNumberList が `[1, 2, 3, 4]` であることをアサートしている
  - [x] 前のページが存在しないことをアサートしている(ページングナビゲーション参照)
  - [x] 次のページが存在することをアサートしている(ページングナビゲーション参照)

## exercise11-要件
対象: 会員ステータスの表示順カラムで会員を並べてカーソル検索()

- [x] 会員ステータスの表示順カラムで会員を並べてカーソル検索できている
  - [x] 会員ステータスの「表示順」カラムの昇順で並べている
  - [x] 会員ステータスのデータも取得している
  - [x] 第二ソートキーとして会員IDの降順で並べている`
  - [x] 会員ステータスが取得できていることをアサートしている
  - [x] 会員が会員ステータスごとに固まって並んでいることをアサートしている 
  - [x] カーソル検索で実装している