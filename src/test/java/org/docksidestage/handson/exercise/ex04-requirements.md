# 要件チェック

## 退会会員の未払い購入を検索()
- [x] 退会会員の未払い購入を検索できている
    - [x] 退会会員のステータスコード `"WDL"` を条件にしている
    - [x] 未払い条件として支払完了フラグ `"0"` を条件にしている
    - [x] 購入日時の降順で並べている
    - [x] 会員名称と商品名をログに出力している
    - [x] 検索結果が1件以上あることをアサートしている(素通りチェック)
    - [x] 検索された購入が未払いであることをアサートしている

## 会員退会情報も取得して会員を検索()
- [x] 会員退会情報も取得して会員を検索できている
    - [x] 退会会員のステータスコード `"WDL"` を条件にしている
    - [x] 検索結果が1件以上あることをアサートしている(素通りチェック)
    - [x] 退会会員でない会員は、会員退会情報を持っていないこと(member_withdrawalにデータがないこと)をアサートしている
    - [x] 不意のバグや不意のデータ不備でもテストが(できるだけ)成り立つようにしている
        - [x] 退会会員でないケースも確認できるようにしている

## memo
- 区分値はCDef.javaから呼び出せる

## 一番若い仮会員の会員を検索()
- [x] 一番若い仮会員の会員を検索できている
    - [x] 仮会員を区分値メソッドで検索している
    - [x] 会員ステータス名称も取得している
    - [x] 会員ステータス名称をログに出力している
    - [x] 検索結果が1件以上あることをアサートしている(素通りチェック)
    - [x] 検索された会員が仮会員であることをアサートしている
    - [x] テストメソッド内の検索回数は一回である

## 支払済みの購入の中で一番若い正式会員のものだけ検索()
- [ ] 支払済みの購入の中で一番若い正式会員のものだけ検索できている
    - [x] 会員ステータス名称も取得している
    - [x] 会員ステータス名称をログに出力している
    - [x] 購入日時の降順で並べている
    - [x] 検索結果が1件以上あることをアサートしている(素通りチェック)
    - [x] 検索された購入に紐づく会員が正式会員であることをアサートしている

## 生産販売可能な商品の購入を検索()
- [ ] 生産販売可能な商品の購入を検索できている
    - [x] 商品ステータス名称を取得している
    - [x] 退会理由テキスト (退会理由テーブル) も取得している
    - [x] 商品ステータス名称と退会理由テキストをログに出力している
    - [x] 値がない項目は `"none"` をログに出力している
    - [x] `"none"` を出すために if 文を使っていない
    - [x] 購入価格の降順で並べている
    - [x] 検索結果が1件以上あることをアサートしている(素通りチェック)
    - [x] 検索された購入に紐づく商品が生産販売可能であることをアサートしている

### memo
こうなる
```
String withdrawalReasonText = "none";
if (purchase.getMember().isPresent()) {
    Member member = purchase.getMember().get();
    if (member.getMemberWithdrawalAsOne().isPresent()) {
        MemberWithdrawal withdrawal = member.getMemberWithdrawalAsOne().get();
        if (withdrawal.getWithdrawalReason().isPresent()) {
            String text = withdrawal.getWithdrawalReason().get().getWithdrawalReasonText();
            if (text != null) {
                withdrawalReasonText = text;
            }
        }
    }
}
```

## 正式会員と退会会員の会員を検索()
- [ ] 正式会員と退会会員の会員を検索できている
    - [x] 会員ステータスの表示順で並べている
    - [x] 検索結果が1件以上あることをアサートしている(素通りチェック)
    - [x] 検索された会員が正式会員または退会会員であることをアサートしている
    - [x] 正式会員と退会会員の両方が存在していることをアサートしている
    - [x] 検索されたデータに対して、Entity上だけで正式会員を退会会員に変更している
    - [x] 変更した後、Entityが退会会員に変更されていることをアサートしている
    - [x] 変更した後、データベース上は退会会員に変更されていないことをアサートしている

### memo
DBFluteは、Entityのデータを変更しても、updateをしない限りDBは不変　へー！

## 銀行振込で購入を支払ったことのある、会員ステータスごとに一番若い会員を検索()
- [x] 銀行振込で購入を支払ったことのある会員の中から、会員ステータスごとに一番若い会員を検索できている
  - [x] 銀行振込 (`PaymentMethod: BankTransfer`) で購入を支払ったことのある会員だけを対象にしている
  - [x] 一回の ConditionBean による検索で会員たちを検索している
  - [x] `PartitionBy...` を活用した検索になっている
  - [x] ログに出た SQL を見て、検索内容が妥当であることを目視確認している
  - [x] 検索結果が、想定されるステータス件数以上であることをアサート(正式会員で一番若い人と、仮会員で一番若い人が1件以上出てきている)している
  - [x] ArrangeQueryバージョンを作れている
